# ./src/index.yaml
# Use the command:
# gcloud app deploy index.yaml
# to update Google Cloud Datastore indexies
indexes:
- kind: Post
  properties:
  - name: public
  - name: date
    direction: desc
